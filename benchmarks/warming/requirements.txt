# Pinned dependencies for the eager-warming demo (issue #168).
# Versions verified against Python 3.13 on macOS/arm64. duckdb ships the `tpch`
# dbgen extension; pyiceberg seeds tables through Polaris (REST) and drives the
# planning walk; boto3 issues the footer suffix-range GETs through Verglas.
duckdb==1.1.3
pyiceberg[pyarrow]==0.8.1
pyarrow==18.1.0
boto3==1.35.99
requests==2.32.3
