# Local venv and generated artifacts — never committed.
.venv/

# Generated verglasd config (carries a short-lived Polaris bearer token).
verglasd.toml

# Daemon log captured during a run.
verglasd.log

# Per-run walk records and the rendered report. Published numbers go on the
# issue/PR, not into the tree (benchmarks/ convention).
results/

# Python bytecode.
__pycache__/
*.pyc

# OCI origin credentials — copied in, never committed (guarded in run.sh too).
.env
