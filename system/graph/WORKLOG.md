# Graph worklog

- #180: Added the prebuilt Turso-backed Graph Worker and Durable Object. Each named graph owns relational nodes, edges, bidirectional adjacency indexes, declared property indexes, replay-stable mutation receipts, and deterministic traversal with hard work ceilings.
- #0: Consume the published JavaScript Worker SDK for component and runtime-surface tests. Graph no longer reaches into an SDK source tree owned by another repository.
