# Dashboard

Dashboard is a stateless Worker project using the JavaScript SDK's semantic JSX
components. It reads declared Query bindings on demand and renders bounded,
escaped HTML and SVG with no client JavaScript, Durable Object, or durable cache.
