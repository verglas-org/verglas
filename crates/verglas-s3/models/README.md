# Service contracts

`s3-openapi.json` is the S3 protocol contract served by the authenticated
frontend. It is kept with the adapter because the frontend and passthrough
implementations are the only public surfaces in this crate.
