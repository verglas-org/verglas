# verglas-celld worklog

Append-only log of changes to this crate, by issue.

- #171: Added the tenant-cell placement foundation for three supervised single-group DO replicas. Placement enforces hard follower and leader resource reservations, prefers the active Worker host only when it has leadership headroom, and otherwise balances by CPU, memory, current leaders, and recent transaction load with deterministic tests.
- #171: Added the supervised child lifecycle safety state machine. Durable children refuse suspension until both transaction archive and checkpoint cover applied state, restored children remain unroutable below the triggering fence, followers may serve only covered snapshots, and only leaders execute stateful Worker events.
- #171: Added the concrete host-local process supervisor. It validates isolated DO identities, creates per-replica data and Unix-socket paths, launches real child processes with host/role arguments, fences stateful and snapshot routing through lifecycle state, kills only after archive/checkpoint gates, and starts restore processes without routing them before recovery finishes. The supervisor does not expose a route until the child has bound a real Unix socket. Host polling reaps unexpected exits and immediately moves the replica behind its applied recovery fence.
- #171: Added the runnable `celld-host` binary and its bounded newline-delimited Unix control protocol. Placement agents can spawn, suspend, restore, finish recovery, and resolve fenced routes through one root-local socket; malformed commands fail without mutating supervision, and daemon shutdown terminates remaining children. Restore now launches the child in the elected target role and rejects a mismatched recovery completion, preventing supervisor and endpoint leadership from diverging.
- #171: Updated the supervised worker naming to `verglasd` after the deployment contract was clarified as one Worker and SQLite engine per isolated microVM. The existing process supervisor remains the lifecycle boundary while microVM provisioning and hard runtime enforcement are connected.
- #171: Updated the child launch wire values to `worker` and `replica`, matching the non-Raft runtime contract. The worker executes stateful events; optional replicas remain durability and fenced-snapshot endpoints only.
- #171: Added per-DO held durability to child specifications and the `SPAWN_WORKER` control command. `celld-host` now passes each worker's replica socket, opaque lease token, generation, and exact recovered start sequence to `verglasd`; invalid role, empty token, or a sequence that disagrees with the lifecycle fence is rejected before process launch.
- #171: Moved the `celld-host` executable target into `verglas-runtime` beside `verglasd` so Cargo can build and expose both binaries to one real-process acceptance test. This crate remains the reusable supervision, lifecycle, placement, and control implementation.
- #171: Extended `SPAWN_WORKER` with an explicit managed-offload directory or `-` for disabled offload. The supervisor passes `--offload-dir` only to enabled workers, allowing catalog DOs to use ordinary worker durability while prohibiting recursive Lakehouse publication.
- #171: Added a strict `PID <do-id>` control query for acceptance diagnostics and process-isolation assertions. It returns only a currently supervised child process and fails for suspended or unknown DOs.
- #0: Extracted the compute substrate behind the `Provisioner` seam, retaining local process behavior as the default and adding a fail-closed Fly Machines configuration skeleton. The supervisor now routes readiness, kill, wait, and reap operations through the seam while preserving archive/checkpoint and restore fencing, covered by a recording-provisioner lifecycle test.
- #0: Added the substrate-neutral `AlarmSchedule` with one replaceable derived deadline per DO, deterministic Tokio-time tests, and supervisor arm/disarm integration. Wake delivery remains advisory: handlers must re-read committed alarm state, and restore re-arming remains at the routing-layer fence extension point.
- #0: Extended local child readiness to a 180-second cold-start budget because component instantiation can take minutes before `verglasd` binds its control socket. Added a regression test proving a delayed child is still published only after it binds.
- #0: Let one logical DO supervise a follower durability endpoint and a Worker endpoint at once. The follower gets a host-local pager key but still launches `verglasd` with the logical DO identity, so replica envelope validation and Worker routing agree without sharing filesystem paths.

<!-- merged from do-engine-171 -->
# verglas-celld worklog

Append-only log of changes to this crate, by issue.

- #171: Added the tenant-cell placement foundation for three supervised single-group DO replicas. Placement enforces hard follower and leader resource reservations, prefers the active Worker host only when it has leadership headroom, and otherwise balances by CPU, memory, current leaders, and recent transaction load with deterministic tests.
- #171: Added the supervised child lifecycle safety state machine. Durable children refuse suspension until both transaction archive and checkpoint cover applied state, restored children remain unroutable below the triggering fence, followers may serve only covered snapshots, and only leaders execute stateful Worker events.
- #171: Added the concrete host-local process supervisor. It validates isolated DO identities, creates per-replica data and Unix-socket paths, launches real child processes with host/role arguments, fences stateful and snapshot routing through lifecycle state, kills only after archive/checkpoint gates, and starts restore processes without routing them before recovery finishes. The supervisor does not expose a route until the child has bound a real Unix socket. Host polling reaps unexpected exits and immediately moves the replica behind its applied recovery fence.
- #171: Added the runnable `celld-host` binary and its bounded newline-delimited Unix control protocol. Placement agents can spawn, suspend, restore, finish recovery, and resolve fenced routes through one root-local socket; malformed commands fail without mutating supervision, and daemon shutdown terminates remaining children. Restore now launches the child in the elected target role and rejects a mismatched recovery completion, preventing supervisor and endpoint leadership from diverging.
- #171: Updated the supervised worker naming to `verglasd` after the deployment contract was clarified as one Worker and SQLite engine per isolated microVM. The existing process supervisor remains the lifecycle boundary while microVM provisioning and hard runtime enforcement are connected.
- #171: Updated the child launch wire values to `worker` and `replica`, matching the non-Raft runtime contract. The worker executes stateful events; optional replicas remain durability and fenced-snapshot endpoints only.
- #171: Added per-DO held durability to child specifications and the `SPAWN_WORKER` control command. `celld-host` now passes each worker's replica socket, opaque lease token, generation, and exact recovered start sequence to `verglasd`; invalid role, empty token, or a sequence that disagrees with the lifecycle fence is rejected before process launch.
- #171: Moved the `celld-host` executable target into `verglas-runtime` beside `verglasd` so Cargo can build and expose both binaries to one real-process acceptance test. This crate remains the reusable supervision, lifecycle, placement, and control implementation.
- #171: Extended `SPAWN_WORKER` with an explicit managed-offload directory or `-` for disabled offload. The supervisor passes `--offload-dir` only to enabled workers, allowing catalog DOs to use ordinary worker durability while prohibiting recursive Lakehouse publication.
- #171: Added a strict `PID <do-id>` control query for acceptance diagnostics and process-isolation assertions. It returns only a currently supervised child process and fails for suspended or unknown DOs.
- #171: Added a structured ManagedCas worker durability variant carrying managed S3 connection fields and the held head ETag/version. `SPAWN_CAS_WORKER` validates the lease fence and launch sequence, then passes the complete configuration through the existing child specification without changing supervision mechanics.
- #171: Added orchestrated suspension with an admission fence, worker DRAIN/CHECKPOINT/STATUS verification, lease-fenced replica coverage and CLEAN, and process termination only after the lifecycle watermark checks pass. Failed orchestration rolls the child back to its running role, while the control socket exposes the new strict `SUSPEND_ORCHESTRATED` command.
- #171: Replaced the orchestration test's Python shell with a small Rust Unix-socket helper binary. The acceptance now records both endpoint commands and verifies fenced routing, checkpoint/coverage/CLEAN ordering, and child termination without an external runtime dependency.
- #171: Extended the supervised-child readiness ceiling to thirty seconds so real process acceptance remains deterministic under cold workspace builds and concurrent test startup.
- #171: Added structured per-worker memory and descriptor ceilings with Unix pre-exec `setrlimit` enforcement, sane defaults, and a dependency-free diagnostic acceptance test. The limit application is marked as the extension point that will move into the future WASM/microVM runtime.
- #171: Kept Darwin worker startup safe because its kernel rejects lowering inherited unlimited address-space/data rlimits; Linux and other supported Unix workers enforce the configured memory ceiling, while every Unix worker enforces the descriptor ceiling.
- #171: Fixed Linux compilation of the worker rlimit enforcement: setrlimit resource
  selectors are u32 (__rlimit_resource_t) on Linux but c_int on macOS, so the
  pre-exec helper now takes a per-platform RlimitResource alias.
- #171: Applied the same per-platform RlimitResource alias to the test helper's
  getrlimit diagnostics so the worker binary also compiles on Linux.
- #171: Added a Cloudflare Workers-shaped management router for multipart script uploads, durable-object namespace metadata, deterministic object IDs, and CF response envelopes. The router stores source modules below the celld root and shares the supervisor with the control socket so object creation, route checks, status, and suspension use real worker lifecycle machinery.
- #0: Unified the replica and managed-CAS worker control shapes, shared celld-host supervision with the management API, and restored orchestrated drain/checkpoint/coverage/clean ordering. Local children now receive hard resource ceilings, with a real helper binary covering the process limits and lifecycle protocol.
- #171: Replaced the deleted durability engine with one Turso Worker launch path carrying the DO id, local data root, remote URL, token file, verified component, cache, and event socket. Removed local replica/CAS ownership and archive/checkpoint orchestration; suspension now requires Turso push, drained outbox, and clean event shutdown confirmations. Tests were written first and initially failed on the removed control grammar/API, then passed after the new parser, forwarding, lifecycle, and fail-closed management paths landed.
- #171: Moved the supervision executable into this crate under its final `verglas-celld` name and changed child documentation and fixtures to launch `verglas-runtime`. The old `celld-host` and `verglasd` executable names are not retained as aliases.
- #171: Added strict `ICEBERG_COMMIT → verglas-runtime` parsing to the `SPAWN_WORKER` control command and carried the validated declaration through `ChildSpec` and `ProvisionRequest`. Celld retains the declaration until the runtime exposes startup inputs, rejecting all other host-service pairs instead of creating a hidden product or forwarding an unsupported CLI flag.
- #171: Removed the unused Fly Machines provisioner skeleton and public re-export because celld has no production callers for that fail-closed surface. Deleted the obsolete test-worker target/source and placeholder orchestration test support now that tests use direct fixtures.
- #0: Added the operator-owned `--catalog-host-config` path and propagated it through the local provisioner only for the exact `ICEBERG_COMMIT → verglas-runtime` declaration. Catalog launches fail closed with a typed error when the path is absent, while non-Catalog launches keep their existing arguments; focused tests witnessed the expected red API failure before implementation and now pass.
- #171: Removed remote Turso URL/token state from celld control, management, supervision, and child provisioning. Celld now assigns the self-hosted data path and component inputs only, and lifecycle language treats the embedded WAL checkpoint plus drained outbox as the shutdown durability fence.
- #171: Added red control coverage for the credential-free `SPAWN_WORKER` grammar, then updated child argv assertions and Catalog host-config tests to prove only local identity, storage, component, cache, socket, and optional host configuration cross the process boundary.
- #171: Added red process tests for a child data-root escape and crash restoration. Celld now requires each Turso database and event socket at the exact cell-root/DO-identity path, and an already-fenced crashed child can launch its replacement without attempting an invalid second lifecycle transition.
- #171: Local-process suspension and host shutdown now send SIGINT instead of SIGKILL. The production runtime can close event admission and complete its Turso/outbox/WAL fence before celld reaps it.
- #171: Added a red process test proving celld previously swallowed a failed child
  shutdown fence. Suspension now moves a failed child behind crash recovery,
  and host shutdown stops every child before returning the first nonzero status.
- #171: Renamed the supervisor crate and executable from the ambiguous
  `verglas-celld` name to `verglasd`. The control socket, package imports,
  diagnostics, and tests use the new name with no binary or CLI alias.
