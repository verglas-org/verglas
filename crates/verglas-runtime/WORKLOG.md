# Worklog

- #171: Created the executable runtime package that owns both `celld-host` and `verglasd`, allowing one integration test to launch the real supervisor and real isolated workers without locating artifacts from another Cargo package. Moved the existing process-level replica failover acceptance test with the binaries so Cargo supplies both executable paths deterministically.
- #171: Added the real local acceptance scenario. It launches two replica `verglasd` processes and the actual `celld-host`, creates an ordinary agent DO and a catalog DO through `SPAWN_WORKER`, commits independent canonical transactions, gracefully stops the host, deletes both worker pagers, restarts the host, and proves fresh workers replay sequence one from their replica services.
- #171: Extended that scenario with real Arrow relational mutations and DataFusion SQL aggregation in both isolated workers. It verifies query results before failure and again after deleting local pagers and replaying into replacement workers, proving each process is an executable SQLite plus DataFusion stack rather than only a durability endpoint.
- #171: Wired archive-enabled `verglasd` workers to a managed object-store root and verified explicit compacted drain in the real-process acceptance test. The same test now proves the catalog worker rejects `DRAIN` and produces no catalog archive object, keeping catalog Lakehouse offload disabled.
- #171: Strengthened process acceptance with distinct worker PID assertions, child-exit diagnostics, a ten-second readiness budget, and RAII cleanup so failed assertions cannot orphan `celld-host` or `verglasd` children. The held-out scenario also proves stale and uncovered replica `CLEAN` requests fail while the committed tail remains replayable.
- #0: Added paired `--component-digest` and `--component-dir` options to `verglasd`, with digest-verified loading and fail-closed startup before the private socket bind. Acceptance tests cover option pairing, corrupt artifacts, and a valid empty component; event protocol dispatch into `WorkerRuntime` remains the explicit follow-up seam. (Replace #0 with the tracking issue number before release.)
- #0: The startup acceptance deliberately uses an empty component because no wasm32 target or component toolchain is installed in this checkout; component instantiation is lazy and therefore deferred until event wiring supplies a real handler. The private Unix protocol still serves the existing engine commands only, so gateway fetch, alarm, and WebSocket commands must be connected in a follow-up. (Replace #0 with the tracking issue number before release.)
- #0: Wired the optional `--cwasm-cache-dir` startup path with fail-closed cache errors and added `EngineWorkerStorage`, whose KV and alarm overlays provide read-your-writes while engine tables remain invisible until commit. SQL returns a typed unsupported capability error rather than a fabricated result. (Replace #0 with the tracking issue number before release.)
- #0: Added real Arrow IPC and JSON-row SQL execution through a caller-owned DataFusion `DoSession`, with staged relational writes visible at the event snapshot and persistence tests across engine reopen. Added the resident NDJSON event socket, commit-gated WebSocket effects, event serialization, startup alarm arming, and advisory self-delivery through the same gate and engine authority. (Replace #0 with the tracking issue number before release.)
- #0: Event endpoint alarm delivery now also works before a gateway connection arrives by retaining committed effects until the next socket writer, and runtime dispatch retains the WIT per-component initialization invariant. (Replace #0 with the tracking issue number before release.)
- #0: JSON-row SQL explicitly preserves nullable fields as JSON null using Arrow's explicit-null array writer mode. (Replace #0 with the tracking issue number before release.)

<!-- merged from do-engine-171 -->
# Worklog

- #171: Created the executable runtime package that owns both `celld-host` and `verglasd`, allowing one integration test to launch the real supervisor and real isolated workers without locating artifacts from another Cargo package. Moved the existing process-level replica failover acceptance test with the binaries so Cargo supplies both executable paths deterministically.
- #171: Added the real local acceptance scenario. It launches two replica `verglasd` processes and the actual `celld-host`, creates an ordinary agent DO and a catalog DO through `SPAWN_WORKER`, commits independent canonical transactions, gracefully stops the host, deletes both worker pagers, restarts the host, and proves fresh workers replay sequence one from their replica services.
- #171: Extended that scenario with real Arrow relational mutations and DataFusion SQL aggregation in both isolated workers. It verifies query results before failure and again after deleting local pagers and replaying into replacement workers, proving each process is an executable SQLite plus DataFusion stack rather than only a durability endpoint.
- #171: Wired archive-enabled `verglasd` workers to a managed object-store root and verified explicit compacted drain in the real-process acceptance test. The same test now proves the catalog worker rejects `DRAIN` and produces no catalog archive object, keeping catalog Lakehouse offload disabled.
- #171: Strengthened process acceptance with distinct worker PID assertions, child-exit diagnostics, a ten-second readiness budget, and RAII cleanup so failed assertions cannot orphan `celld-host` or `verglasd` children. The held-out scenario also proves stale and uncovered replica `CLEAN` requests fail while the committed tail remains replayable.
- #171: Added the managed-CAS runtime variant with explicit S3 endpoint, bucket, prefix, region, and credentials wiring. `verglasd` validates the held ETag/version, restores a verified checkpoint plus contiguous CAS tail before binding, and the new real-process acceptance covers sequence continuation and stale-worker fencing.
- #171: Completed replica-CLEAN recovery across shared local object storage. Replica workers discover and hash-verify the newest checkpoint before replaying the remaining durable tail, publish checkpoint coverage to the replica service, and preserve remote archive watermarks after replacement.
- #171: Hardened the strict S3 test endpoint with ListObjectsV2 timestamp output and multi-object-delete responses, and made endpoint response shutdown tolerant of an already-closed Unix peer.
- #171: Replaced a stale local replica pager with the newest verified checkpoint before replay when replica CLEAN has already removed its tail, including SQLite WAL sidecars.
- #171: Added the real all-domain process acceptance. One canonical socket commit advances relational, Vamana, and graph watermarks together; deterministic vector search, graph neighbors, exact retry/conflict fencing, durable index declarations, and replacement reconstruction all pass through `celld-host` and `verglasd`.
- #171: Added the optional `--management-bind` TCP listener to `celld-host` and wired it to the shared supervisor, leaving the listener disabled unless explicitly configured. The runtime now serves the celld Workers management API beside the existing Unix control protocol.
- #0: Reunified verglasd's verified component loading, compiled cache, and event socket with both replica and managed-CAS authorities. Event transactions now use the configured engine authority, with effects released only after the terminal event commit path.
- #171: Completed the service-world runtime migration and added bounded, reader-serviced DO subrequests. Each event socket tracks up to 16 pending call ids, routes do-call-result frames through oneshots, propagates gateway errors, forwards guest WebSocket acceptance, and documents that subrequests are deliberately ungated while socket effects remain commit-gated.
- #0: TDD baseline for the Turso runtime swap was recorded before implementation. `cargo test -p verglas-runtime --features test-support --test worker_storage` failed with unresolved `verglas_do_engine`, `EngineWorkerStorage`, and removed Arrow imports after the new Turso tests and dependency were staged; these are the expected swap failures.
- #0: Replaced `EngineWorkerStorage` with `TursoWorkerStorage`, which keeps the WIT WorkerStorage surface on one Turso event transaction, rejects Arrow IPC SQL with a typed hard error, rolls back handler failures, commits and pushes before releasing effects, and drains an injected Stream outbox after acknowledgement. `verglasd` now fails closed unless explicit Turso URL/token-file/data-root configuration is present; obsolete CAS, replica, checkpoint, offload, and S3 authority code and acceptance tests were deleted.
- #0: Implemented PP4's crash-gap-free Stream relay and removed the Arrow SQL capability. Durable Object sends are staged through WIT storage, the runtime appender posts producer identities to the internal Stream route and waits for 2xx ACK, active leases apply serialized-event backpressure, expired claims reclaim without sleeps, and endpoint tests prove failed publication holds WebSocket effects and the next event.
- #0: Added a live endpoint kill test that exercises the injected binding-backed Stream appender, verifies the internal route and producer identity, and proves WebSocket effects remain withheld until the durable 2xx acknowledgement arrives.
- #171: Renamed the sole Turso-backed Wasmtime worker executable from `verglasd` to `verglas-runtime` without retaining an alias. Process tests and static inventory guards now enforce the final name and reject the retired cache-node, clustering crates, and old runtime source.
- #171: Added a static architecture guard after witnessing the old whitepaper fail on its clustered storage design. Replaced that obsolete design with the six-product gateway, celld, runtime, Turso, Foyer, origin, and narrow Iceberg capability architecture; the guard now passes.
- #171: Added the runtime-owned `ICEBERG_COMMIT` interceptor over the existing `do-fetch` ABI. Exact binding/object/method/path calls invoke a bounded host service backed by `commit_sink_batch`, malformed reserved calls fail before socket routing, and all unrelated bindings keep their existing gateway DO-call path; host catalog and storage credentials never enter the component.
- #171: Expanded the dead-code inventory to reject the removed standalone Catalog server/authz/IO/extension crates and the final core node/peer/ring modules. Catalog remains only the Turso-backed WASM product plus the narrow runtime Iceberg helper.
- #171: Replaced the runtime Catalog authority with a stateless proposal writer that builds request-scoped FileIO from a host-owned StorageFactory. It accepts create-table and commit-sink-batch envelopes, writes Parquet and Iceberg metadata files without retaining a Catalog head, and returns full metadata proposals for SQLite publication.
- #171: Added standard commit-table proposals for PyIceberg. The runtime now preserves the exact REST requirements and updates document, validates and applies it through patched Iceberg TableCommit::from_parts/apply, writes the next metadata location, and returns the standard metadata-location plus full metadata response.
- #171: Added the host-owned origin StorageFactory, which fixes one binding/bucket route, reads through PassthroughRead and Foyer with persistent range/version-aware cache keys, and fences durable origin writes before exact mapping invalidation. Focused integration tests cover origin fill and hit, NVMe reopen, write ordering, streamed writes, and binding/bucket isolation.
- #171: Added strict JSON Catalog host startup configuration for the exact origin/backend route, Foyer hard budgets, warehouse prefix, and Sink fence. Runtime startup now constructs the BackendStore, origin factory, and Iceberg commit capability before selecting the privileged endpoint bind, while ordinary children retain the normal bind path; invalid or unreadable configuration fails closed before the socket.
- #171: Tightened privileged storage startup to one exact bucket with no wildcard scope, probe the origin before opening Foyer or the event socket, and reject ambiguous warehouse, metadata, and object-key paths. Focused tests cover rejected probe credentials, traversal and control characters, and the requirement that host configuration accompany a verified component event socket.
- #171: Added an architecture guard for retired standalone management, generic-table, and query API specifications. Removed those unreferenced surfaces and updated the documentation summary to describe Worker and Durable Object WASM products instead of a standalone lakehouse server.
- #171: Expanded capability coverage across streaming immutable writes, exact deletes, typetag fail-closed restoration, strict host configuration variants, and all admitted Sink primitive columns. Negative cases exercise schema drift, unsupported nested values, required columns, invalid cache budgets, and ambiguous routes rather than adding assertion-free coverage.
- #171: Fixed nullable Sink primitive conversion after the new mixed-type acceptance test failed because an explicit JSON null was treated as an invalid string. Boolean, integer, float, and string arrays now preserve explicit nulls when the inferred Iceberg field is nullable, while non-null type drift remains a 400 response.
- #171: Removed remote Turso URL and token arguments from the production runtime. Each child now opens only its embedded database under the celld-assigned data root, commits through the local checkpoint fence, and rejects the retired deployment options as unknown.
- #171: Removed the unused reverse Cargo dependency from runtime to celld. Celld launches runtime only through the process/control contract, so the runtime dependency graph no longer points back into its supervisor.
- #171: Routed the Catalog's stable logical bucket through an exact deployment-owned physical bucket. This keeps the system Worker component reusable across deployments while the host retains the only mapping to provider storage.
- #171: Runtime signal shutdown now drops the event listener before invoking Turso's explicit shutdown fence. Process exit therefore requires no abandoned transaction, no undelivered Stream outbox row, and a complete embedded WAL checkpoint.
- #171: Updated the dependency-direction guard for the supervisor's final
  `verglasd` package name. Runtime remains independent and is launched only
  through the process contract.
- #175: Live Durable Object children now require exact per-object S3-CAS configuration and open Turso through it. Turso and Catalog Iceberg storage share one Foyer instance, while an explicit build-only mode serializes components without opening storage or binding an event socket.
- #0: Move public architecture assertions with the documentation to verglas-sdk. The runtime repository no longer reaches into a removed Mintlify tree during tests.
