# Worklog

- #171: Created the executable runtime package that owns both `celld-host` and `verglasd`, allowing one integration test to launch the real supervisor and real isolated workers without locating artifacts from another Cargo package. Moved the existing process-level replica failover acceptance test with the binaries so Cargo supplies both executable paths deterministically.
- #171: Added the real local acceptance scenario. It launches two replica `verglasd` processes and the actual `celld-host`, creates an ordinary agent DO and a catalog DO through `SPAWN_WORKER`, commits independent canonical transactions, gracefully stops the host, deletes both worker pagers, restarts the host, and proves fresh workers replay sequence one from their replica services.
- #171: Extended that scenario with real Arrow relational mutations and DataFusion SQL aggregation in both isolated workers. It verifies query results before failure and again after deleting local pagers and replaying into replacement workers, proving each process is an executable SQLite plus DataFusion stack rather than only a durability endpoint.
- #171: Wired archive-enabled `verglasd` workers to a managed object-store root and verified explicit compacted drain in the real-process acceptance test. The same test now proves the catalog worker rejects `DRAIN` and produces no catalog archive object, keeping catalog Lakehouse offload disabled.
- #171: Strengthened process acceptance with distinct worker PID assertions, child-exit diagnostics, a ten-second readiness budget, and RAII cleanup so failed assertions cannot orphan `celld-host` or `verglasd` children. The held-out scenario also proves stale and uncovered replica `CLEAN` requests fail while the committed tail remains replayable.
