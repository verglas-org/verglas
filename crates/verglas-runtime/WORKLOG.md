# Worklog

- #171: Created the executable runtime package that owns both `celld-host` and `verglasd`, allowing one integration test to launch the real supervisor and real isolated workers without locating artifacts from another Cargo package. Moved the existing process-level replica failover acceptance test with the binaries so Cargo supplies both executable paths deterministically.
- #171: Added the real local acceptance scenario. It launches two replica `verglasd` processes and the actual `celld-host`, creates an ordinary agent DO and a catalog DO through `SPAWN_WORKER`, commits independent canonical transactions, gracefully stops the host, deletes both worker pagers, restarts the host, and proves fresh workers replay sequence one from their replica services.
- #171: Extended that scenario with real Arrow relational mutations and DataFusion SQL aggregation in both isolated workers. It verifies query results before failure and again after deleting local pagers and replaying into replacement workers, proving each process is an executable SQLite plus DataFusion stack rather than only a durability endpoint.
- #171: Wired archive-enabled `verglasd` workers to a managed object-store root and verified explicit compacted drain in the real-process acceptance test. The same test now proves the catalog worker rejects `DRAIN` and produces no catalog archive object, keeping catalog Lakehouse offload disabled.
- #171: Strengthened process acceptance with distinct worker PID assertions, child-exit diagnostics, a ten-second readiness budget, and RAII cleanup so failed assertions cannot orphan `celld-host` or `verglasd` children. The held-out scenario also proves stale and uncovered replica `CLEAN` requests fail while the committed tail remains replayable.
- #0: Added paired `--component-digest` and `--component-dir` options to `verglasd`, with digest-verified loading and fail-closed startup before the private socket bind. Acceptance tests cover option pairing, corrupt artifacts, and a valid empty component; event protocol dispatch into `WorkerRuntime` remains the explicit follow-up seam. (Replace #0 with the tracking issue number before release.)
- #0: The startup acceptance deliberately uses an empty component because no wasm32 target or component toolchain is installed in this checkout; component instantiation is lazy and therefore deferred until event wiring supplies a real handler. The private Unix protocol still serves the existing engine commands only, so gateway fetch, alarm, and WebSocket commands must be connected in a follow-up. (Replace #0 with the tracking issue number before release.)
- #0: Wired the optional `--cwasm-cache-dir` startup path with fail-closed cache errors and added `EngineWorkerStorage`, whose KV and alarm overlays provide read-your-writes while engine tables remain invisible until commit. SQL returns a typed unsupported capability error rather than a fabricated result. (Replace #0 with the tracking issue number before release.)
- #0: Added real Arrow IPC and JSON-row SQL execution through a caller-owned DataFusion `DoSession`, with staged relational writes visible at the event snapshot and persistence tests across engine reopen. Added the resident NDJSON event socket, commit-gated WebSocket effects, event serialization, startup alarm arming, and advisory self-delivery through the same gate and engine authority. (Replace #0 with the tracking issue number before release.)
- #0: Event endpoint alarm delivery now also works before a gateway connection arrives by retaining committed effects until the next socket writer, and runtime dispatch retains the WIT per-component initialization invariant. (Replace #0 with the tracking issue number before release.)
- #0: JSON-row SQL explicitly preserves nullable fields as JSON null using Arrow's explicit-null array writer mode. (Replace #0 with the tracking issue number before release.)
