# verglas-do-wasm worklog

- #0: Added the Durable Object Worker ABI bindings, event output gate, and content-addressed component artifact store. This establishes the host seams for verglasd while keeping staged socket effects invisible until durable commit. (Replace #0 with the tracking issue number before release.)
- #0: Added `WorkerRuntime` for async Wasmtime component compilation, per-event stores, generated handler dispatch, and explicit pending permits. Refactored the output gate around a cloneable `StagingSockets` sink so guest sends and closes remain invisible until the caller commits. (Replace #0 with the tracking issue number before release.)
- #0: Runtime tests use a real Wasmtime `(component)` fixture for compile/startup and exercise the full EventGate adapter with mocked capabilities. A fetch round-trip component was deferred because this checkout has no wasm32 target or component toolchain, and hand-writing the WIT canonical-ABI component export/import WAT was not practical; dispatch still fails closed when the empty fixture has no handler. (Replace #0 with the tracking issue number before release.)
- #0: Added the documented WIT `init` export and fail-closed initialization dispatch, plus a digest and Wasmtime compatibility-keyed `CwasmCache` that never silently falls back when its configured directory is unusable. (Replace #0 with the tracking issue number before release.)
- #0: Linked locked-down WASI Preview 2 imports into each Worker event store while inheriting only stdout and stderr for guest diagnostics. The runtime test proves a component importing `wasi:clocks/wall-clock` instantiates through the shared linker. (Replace #0 with the tracking issue number before release.)
- #0: Added the frozen `storage.sql-rows` WIT verb and propagated its JSON-array encoding through WorkerStorage and WorkerHost, while keeping native Arrow IPC SQL as the separate execution surface. Ordinary event dispatch now requires explicit one-time initialization before fetch, alarm, and WebSocket calls. (Replace #0 with the tracking issue number before release.)
- #0: Preserved the ABI rule that `init` runs once for every fresh component instance; verglasd also performs an explicit startup init wake before accepting frames, while ordinary runtime dispatch keeps its fail-closed component-local init hook. (Replace #0 with the tracking issue number before release.)
- #0: Landed the v2 service world with stateless Worker exports and flattened Durable Object bindings, plus `WorkerPool` fresh-store execution that rejects storage and socket access honestly while routing `do-fetch` through `DoRouter`. (Replace #0 with the tracking issue number before release.)
- #0: Renamed all generated service-world consumers from the stale `DurableObject` Rust alias to `Service`, keeping WorkerRuntime and WorkerPool aligned with the normative `world service` contract. (Replace #0 with the tracking issue number before release.)
- #171: Threaded `WorkerBindings` through every gated WorkerRuntime event so the runtime endpoint can route cross-object calls through its per-socket host channel without retaining a compatibility alias. (Replace #0 with the tracking issue number before release.)
- #0: Updated the frozen WIT for PP4: Arrow IPC SQL was deleted, JSON-row SQL is the sole relational surface, and `storage.stream-send` stages logical records in the current event transaction. WorkerHost and stateless WorkerPool now implement the narrowed capability without a compatibility verb.
- #171: Updated resident-host diagnostics to name the final `verglas-runtime` executable after the old worker daemon name was removed. The WASM capability model and no-network isolation contract are unchanged.
- #171: Updated the service-world source comment to identify `verglas-runtime` as the sole host. The WIT surface and capability boundaries are unchanged.
