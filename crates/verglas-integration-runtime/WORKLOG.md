# Worklog

- #43: Added the generated Integration API contract, self-published reflection manifests, bounded and NDJSON-streaming invocation, and authenticated SDK injection through `this.verglas`.
- #107: Cut queue enqueue injection over to the independent queue-service message envelope; the integration runtime no longer emits the retired local segment-log body.
