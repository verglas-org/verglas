[package]
name = "verglas-api"
description = "Dependency-leaf request and response contracts shared by Verglas clients and engines"
version.workspace = true
edition.workspace = true
repository.workspace = true
# Published to crates.io as the SDK's contract types; overrides the workspace-wide
# publish = false (which exists because the engine crates depend on the
# patched Iceberg fork — this crate does not).
publish = true
license-file.workspace = true

[dependencies]
serde.workspace = true
serde_json.workspace = true

[lints]
workspace = true
