# Worklog

- #42: Added a custom Rill build that registers Verglas as a live OLAP store with Arrow results, typed parameters, and information-schema discovery. The driver owns no models or dashboard state; Rill project files define those resources directly.
