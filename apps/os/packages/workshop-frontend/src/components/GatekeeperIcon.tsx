/** Gatekeepers removed — stub icon that accepts residual prop shapes. */
export function GatekeeperIcon(_props: {
  vendorId?: string
  className?: string
  size?: number | string
  logoUrl?: string
  color?: string
  fallbackText?: string
}) {
  return null
}
