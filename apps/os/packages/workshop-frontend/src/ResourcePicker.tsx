/** Gatekeepers removed — type stub for residual ChatInterface imports. */
export interface VendorOption {
  vendorId: string
  displayName: string
}

export type SelectableItem = {
  kind: string
  id: string
  label: string
  [key: string]: unknown
}

export interface ResourcePickerProps {
  [key: string]: unknown
}

export default function ResourcePicker(_props: ResourcePickerProps) {
  return null
}
