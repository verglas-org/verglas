# Pinned dependencies for the engine-matrix result-diffing harness (issue #23).
# Versions verified against Python 3.13 (macOS/arm64 and ubuntu x86_64). The
# harness reads the same Iceberg fixture two ways per engine — direct to MinIO
# and through the Verglas endpoint — and diffs the results at the Arrow level.
#
# duckdb  ships the `iceberg` extension (one engine of the matrix).
# pyiceberg writes the fixture (Parquet data files) and feeds Polars its scans.
# polars   is the second engine (pyiceberg scan -> Arrow -> Polars).
# pyarrow  is the lingua franca the diff runs over.
duckdb==1.1.3
pyiceberg[sql-sqlite,pyarrow]==0.8.1
pyarrow==18.1.0
polars==1.17.1
