# Pin the docs toolchain to MkDocs 1.x. MkDocs 2.0 removes the plugin/theme
# system and breaks mkdocs-monorepo-plugin; bump manually + re-validate.
# (Also enforced via mkdocs-material's own `mkdocs<2` cap and a renovate rule.)
mkdocs>=1.6,<2
mkdocs-autorefs
mkdocs-material==9.7.6
mkdocs-material-extensions==1.3.1
mkdocs-monorepo-plugin @ git+https://github.com/bitsondatadev/mkdocs-monorepo-plugin@url-fix
markdown-grid-tables==0.6.0
mkdocs-exclude-search==0.6.6
