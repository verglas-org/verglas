# Heavy deps for the pipeline (Silver/Gold), the agent, and the UI.
# CPU-only torch to keep the image sane.
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.3.1
torchvision==0.18.1
open_clip_torch>=2.24.0
pyiceberg[pyarrow]>=0.7.0
pillow>=10.0.0
ollama>=0.3.0
jupyterlab>=4.2.0
ipywidgets>=8.1.0
