{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": [
    "config:recommended"
  ],
  "prHourlyLimit": 3,
  "ignoreDeps": [
    "lakekeeper-console",
    "iceberg",
    "iceberg-ext",
    "iceberg-catalog-rest",
    "azure_core",
    "azure_identity",
    "azure_storage_blob",
    "azure_storage_datalake",
    "rust"
  ],
  "packageRules": [
    {
      "description": "Don't auto-bump CI service/runner versions. postgres (the catalog DB) and the ubuntu runner OS define the test environment, not the dependency tree — major bumps should be a deliberate, reviewed choice (does CI still reflect the versions we support?), not an automatic PR.",
      "matchDepNames": ["postgres", "ubuntu"],
      "enabled": false
    },
    {
      "description": "Group all GitHub Actions updates (digest, minor, major) into one PR",
      "matchManagers": ["github-actions"],
      "groupName": "github-actions",
      "groupSlug": "github-actions"
    },
    {
      "description": "Group non-major Rust crate updates (1.x crates)",
      "matchManagers": ["cargo"],
      "matchUpdateTypes": ["minor", "patch"],
      "matchCurrentVersion": ">=1.0.0",
      "groupName": "rust crates (non-major, 1.x+)",
      "groupSlug": "cargo-stable-minor-patch"
    },
    {
      "description": "Group pre-1.0 Rust crate bumps separately — Cargo treats 0.x→0.y as breaking even though Renovate calls it minor",
      "matchManagers": ["cargo"],
      "matchUpdateTypes": ["minor", "patch"],
      "matchCurrentVersion": "<1.0.0",
      "groupName": "rust crates (pre-1.0)",
      "groupSlug": "cargo-pre1-minor-patch"
    },
    {
      "description": "Docs toolchain is pinned to MkDocs 1.x — 2.0 removes the plugin/theme system and breaks the monorepo-plugin fork. Bump manually and re-validate the docs build.",
      "matchManagers": ["pip_requirements"],
      "matchPackageNames": ["mkdocs", "mkdocs-material", "mkdocs-material-extensions", "mkdocs-autorefs", "mkdocs-exclude-search", "mkdocs-monorepo-plugin", "markdown-grid-tables"],
      "enabled": false
    }
  ]
}