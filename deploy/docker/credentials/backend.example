# Copy to `backend` (no extension) and fill in. Mounted at
# /etc/verglas/credentials/backend — mode 0600 on the host.
# Point [backend].credentials_file at it in verglas.toml.

[default]
aws_access_key_id =
aws_secret_access_key =
